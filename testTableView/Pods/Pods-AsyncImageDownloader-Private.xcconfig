#include "Pods-AsyncImageDownloader.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/AsyncImageDownloader" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/AsyncImageDownloader" "${PODS_ROOT}/Headers/JGProgressHUD"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}